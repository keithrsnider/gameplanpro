services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: localpassword
      POSTGRES_DB: gameplanpro
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
